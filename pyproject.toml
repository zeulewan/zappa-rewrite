[project]
name = "zappa-proxy"
version = "0.1.0"
description = "AI-powered mitmproxy addon that rewrites web assets through Cerebras."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
